{
    "The Sieve of Eratosthenes": {
        "scope": "javascript,typescript",
        "prefix": "primesGenerator",
        "body": [
            "// The Sieve of Eratosthenes",
            "function generatePrimes(n) {",
            "\t// create array of size n",
            "\tvar primes = [];",
            "\tfor(var i = 0; i < n; i++) {",
            "\t\tprimes.push(true);",
            "\t}",
            "\t// 0 and 1 are not prime",
            "\tprimes[0] = false;",
            "\tprimes[1] = false;",
            "",
            "\t// loop through all numbers from 2 to sqrt(n)",
            "\tvar sqrtN = Math.sqrt(n);",
            "\tfor(var i = 2; i <= sqrtN; i++) {",
            "\t\t// if i is prime",
            "\t\tif(primes[i]) {",
            "\t\t\t// loop through all multiples of i and mark them as not prime",
            "\t\t\tfor(var j = i * i; j < n; j += i) {",
            "\t\t\t\tprimes[j] = false;",
            "\t\t\t}",
            "\t\t}",
            "\t}",
            "",
            "\t// return all primes",
            "\tvar result = [];",
            "\tfor(var i = 0; i < n; i++) {",
            "\t\tif(primes[i]) {",
            "\t\t\tresult.push(i);",
            "\t\t}",
            "\t}",
            "\treturn result;",
            "}"
        ]
    },
    "Prime factorization": {
        "scope": "javascript,typescript",
        "prefix": "primeFactorization",
        "body": [
            "// Simple prime factorization",
            "function primeFactors(n) {",
            "\t// array to hold all the prime factors",
            "\tvar factors = [];",
            "\t// check all natural numbers from 2 to square root of n",
            "\tvar sqrtN = Math.sqrt(n);",
            "\tfor (var i = 2; i <= sqrtN; i++) {",
            "\t\t// check how manu times i divides n",
            "\t\twhile (n % i === 0) {",
            "\t\t\t// add i to the array",
            "\t\t\tfactors.push(i);",
            "\t\t\t// divide n by i",
            "\t\t\t// use floor division to get the integer",
            "\t\t\tn = Math.floor(n / i);",
            "",
            "\t\t\t// if n is 1, then we are done",
            "\t\t\tif (n == 1) {",
            "\t\t\t\treturn factors;",
            "\t\t\t}",
            "\t\t}",
            "\t}",
            "",
            "\t// if n is greater than 1, then add n to the array",
            "\tif (n > 1) {",
            "\t\tfactors.push(n);",
            "\t}",
            "",
            "\treturn factors;",
            "}"
        ],
        "description": "Prime factorization of number"
    },
    "Check if number is prime": {
        "scope": "javascript,typescript",
        "prefix": "isPrime",
        "body": [
            "// Check if number is prime",
            "function isPrime(n) {",
            "\t// if number is less than 2 or not an integer then by definition cannot be prime",
            "\tif (n < 2 || n % 1 != 0) {",
            "\t\treturn false;",
            "\t}",
            "\t// otherwise check if divisible by any number under the square root of the number",
            "\t// if so then it is not prime",
            "\tfor (var i = 2; i <= Math.sqrt(n); i++) {",
            "\t\tif (n % i == 0) {",
            "\t\t\treturn false;",
            "\t\t}",
            "\t}",
            "\t// otherwise it is prime",
            "\treturn true;",
            "}"
        ],
        "description": "Check if number is prime"
    },
    "Fibonacci number": {
        "scope": "javascript,typescript",
        "prefix": "fibonacci",
        "body": [
            "// Get n Fibonacci number",
            "function fib(n) {",
            "\t// if n is 0 or 1, return 1",
            "\tif (n <= 1) {",
            "\t\treturn n;",
            "\t}",
            "\t// otherwise, return the sum of the previous two numbers",
            "\treturn fib(n - 1) + fib(n - 2);",
            "}"
        ],
        "description": "Get n Fibbonacci number"
    },
    "Fibonacci array": {
        "scope": "javascript,typescript",
        "prefix": "fibonacciArray",
        "body": [
            "// Get array of n fibonacci numbers",
            "// n > 2",
            "function fibonacciArray(n) {",
            "\tvar fibonacci = [0, 1];",
            "\tfor (var i = 2; i < n; i++) {",
            "\t\tfibonacci[i] = fibonacci[i - 1] + fibonacci[i - 2];",
            "\t}",
            "\treturn fibonacci;",
            "}"
        ],
        "description": "Get n Fibbonacci numbers"
    },
    "Greatest common divisor": {
        "scope": "javascript,typescript",
        "prefix": "gcd",
        "body": [
            "// greatest common divisor algorithm",
            "function gcd(a, b) {",
            "\twhile(a !== b) {",
            "\t\tif(a < b) {",
            "\t\t\tb -= a;",
            "\t\t} else {",
            "\t\t\ta -= b;",
            "\t\t}",
            "\t}",
            "\treturn a;",
            "}"
        ],
        "description": "Greatest common divisor"
    },
    "Co-prime numbers": {
        "scope": "javascript,typescript",
        "prefix": "coprime",
        "body": [
            "// relatively prime numbers or coprime numbers",
            "function isRelativelyPrime(a, b) {",
            "\t// numbers are coprime if their greatest common divisor is 1",
            "\treturn gcd(a, b) === 1;",
            "}"
        ],
        "description": "Check if two numbers are co-prime"
    },
    "Least common multiple": {
        "scope": "javascript,typescript",
        "prefix": "lcm",
        "body": [
            "// least common multiple",
            "function lcm(a, b) {",
            "\t// LCM of any number with 0 is 0",
            "\tif (a == 0 || b == 0) {",
            "\t\treturn 0;",
            "\t}",
            "",
            "\tvar max = Math.max(a, b);",
            "\tvar min = Math.min(a, b);",
            "\tvar lcm = max;",
            "\t// if lcm is divisible by min, then return lcm",
            "\twhile (lcm % min != 0) {",
            "\t\t// increment lcm by max and check again",
            "\t\tlcm += max;",
            "\t}",
            "\treturn lcm;",
            "}"
        ],
        "description": "Least common multiple"
    }
}